name: CITest

on:
  pull_request:
    types:
      - labeled

if: github.event.label.name == 'autotest'

jobs:
  show_message:
    runs-on: ubuntu-latest
    steps:
      - id: messages
        run: |
          echo "Success"
